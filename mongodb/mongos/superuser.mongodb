use('admin')

// need to wait a few second to be primary then run the code below
db.createUser({
    user: "tom-mongos",
    pwd: "jerry",
    roles: ["root"]
})
db.auth('tom', 'jerry')

if (db.getUsers().users.length != 1) {
    print('WARNING: expected only one user, but there are more than one')
}
